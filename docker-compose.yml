version: '3.9'

services:
  postgres:
    image: postgis/postgis:16-3.4
    container_name: nexus_sentinel_db
    environment:
      POSTGRES_USER: ${DB_USER:-nexus_admin}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-sentinel_secure_2024}
      POSTGRES_DB: ${DB_NAME:-nexus_sentinel}
      POSTGRES_INITDB_ARGS: "-c timezone=UTC"
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-nexus_admin}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - nexus_network
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: nexus_sentinel_backend
    environment:
      DATABASE_URL: postgresql+asyncpg://${DB_USER:-nexus_admin}:${DB_PASSWORD:-sentinel_secure_2024}@postgres:5432/${DB_NAME:-nexus_sentinel}
      ENVIRONMENT: ${ENVIRONMENT:-development}
      API_TITLE: ${API_TITLE:-Nexus SENTINEL API}
      API_VERSION: ${API_VERSION:-1.0.0}
      DEBUG: ${DEBUG:-false}
    ports:
      - "${API_PORT:-8000}:8000"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./backend:/app
    networks:
      - nexus_network
    restart: unless-stopped
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8000/api/v1}
        VITE_MAP_TILE_URL: ${VITE_MAP_TILE_URL:-https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png}
    container_name: nexus_sentinel_frontend
    ports:
      - "${FRONTEND_PORT:-3000}:5173"
    depends_on:
      - backend
    volumes:
      - ./frontend/src:/app/src
    networks:
      - nexus_network
    restart: unless-stopped
    environment:
      VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8000/api/v1}
      VITE_MAP_TILE_URL: ${VITE_MAP_TILE_URL:-https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png}

volumes:
  postgres_data:
    driver: local

networks:
  nexus_network:
    driver: bridge
